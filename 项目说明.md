# AI 教学助手 (AIAssite) V3.1 — 项目说明

Copyright (c) 2026 YINJUN. 仅授权个人使用，商业用途需联系版权人。

---

## 项目结构

```
AIAssite/
├── main.py                         # 程序入口（悬浮球 / 托盘 / 设置装配）
├── requirements.txt                # Python 依赖
├── build_spec.spec                 # PyInstaller 打包配置
├── version_info.py                 # exe 版本资源（右键属性可见）
├── 项目说明.md                      # 本文件
├── config/
│   ├── default_settings.json       # 默认配置模板（13 学科提示词 + 开场白 + 课程表）
│   └── settings.json               # 用户配置（运行时生成）
├── core/
│   ├── config.py                   # 配置加载（原子写入）与旧版本迁移
│   ├── subject_defaults.py         # 学科元数据：列表、别名归一、默认提示词与开场白
│   ├── schedule.py                 # 课程表：Excel 导入/导出/模板、时间匹配、自动切换调度
│   ├── ai_client.py                # AI API 流式调用（OpenAI 兼容接口）
│   ├── app_controller.py           # 主控制器（流式请求调度、课程表切换落盘）
│   ├── baidu_auth.py               # 百度 OAuth 认证（ASR/TTS 共用）
│   ├── baidu_asr_client.py         # 百度语音识别（VAD 录音器 + raw 模式）
│   ├── baidu_tts_client.py         # [可选] 百度语音合成（云端引擎）
│   ├── edge_tts_client.py          # [默认] 微软 edge-tts 语音合成（联网）
│   ├── tts_text.py                 # 播报文本处理（Markdown 清理、按句切分）
│   └── recorder.py, asr_client.py  # 录音基础件；Whisper 识别（已弃用）
├── ui/
│   ├── floating_ball.py            # 悬浮球（可拖拽、状态指示灯）
│   ├── satellite_menu.py           # 卫星菜单（环形展开动画）
│   ├── chat_panel.py               # AI 对话面板（流式输出、语音控制、开场白浮层）
│   ├── settings_dialog.py          # 设置对话框（7 页签）
│   └── mini_browser.py             # 内置浏览器（可选）
├── assets/
│   └── icons/
│       ├── AI.png                  # AI 头像图标
│       ├── user.png                # 用户头像图标
│       └── soft.png                # 软件图标
└── pack/                           # 封装工具目录
    ├── setup.nsi                   # NSIS 安装脚本（UTF-8 BOM）
    ├── install.bat                 # 快速安装脚本
    ├── install-auto-start.bat      # 开机自启动注册
    ├── uninstall-auto-start.bat    # 开机自启动移除
    ├── LICENSE                     # 授权协议（个人使用）
    ├── icon.ico                    # 安装程序图标
    ├── welcome.bmp / header.bmp    # NSIS 界面图片
    ├── nssm.exe                    # 服务管理工具（备用）
    ├── 打包指南.txt                 # 详细打包说明
    └── 图标文件准备说明.txt          # 图标制作指南
```

## 配置结构

```json
{
  "ai": {
    "base_url": "http://172.19.21.13:8000/v1",
    "api_key": "",
    "model": "deepseek-ai/DeepSeek-V4-Flash",
    "temperature": 0.7,
    "max_tokens": 2048
  },
  "panel": {
    "width": 420,
    "active_subject": "信息科技",
    "prompts_version": 2,
    "subject_prompts": {
      "语文": "你是一位高中语文老师的教学助手……",
      "外语": "你是一位高中外语老师的教学助手，精通英语、日语、俄语、西班牙语……",
      "数学": "……", "物理": "……", "化学": "……", "生物": "……",
      "地理": "……", "历史": "……", "政治": "……",
      "信息科技": "……", "人工智能通识": "……", "音乐": "……", "美术": "……",
      "通用": "你是一位高中课堂的通用教学助手，不绑定具体学科……"
    },
    "subject_greetings": {
      "语文": ["《红楼梦》里林黛玉进贾府的路线，为什么写得这么细？", "……"],
      "信息科技": ["Python 里的列表和元组到底有什么区别？", "……"],
      "通用": ["自习课上几科作业堆在一起，该怎么安排顺序？", "……"]
    }
  },
  "schedule": {
    "enabled": true,
    "lead_minutes": 3,
    "entries": [
      { "day": 1, "start": "08:00", "end": "08:45", "subject": "语文" },
      { "day": 1, "start": "08:50", "end": "09:35", "subject": "通用", "label": "体育" }
    ]
  },
  "baidu_asr": {
    "api_key": "", "secret_key": "",
    "dev_pid": 1537, "format": "pcm", "rate": 16000, "channel": 1,
    "cuid": "AIAssistant", "asr_url": "https://vop.baidu.com/server_api",
    "silence_threshold": 400, "silence_secs": 3.0
  },
  "tts": {
    "engine": "edge",
    "edge_voice": "zh-CN-XiaoxiaoNeural",
    "baidu": { "api_key": "", "secret_key": "", "per": 4, "spd": 5, "pit": 5, "vol": 5,
               "aue": 3, "cuid": "AIAssistant", "lan": "zh", "ctp": 1,
               "tts_url": "https://tsn.baidu.com/text2audio" }
  },
  "greetings": ["……旧版全局兜底开场白（V3.1 起请在 panel.subject_greetings 中维护）"],
  "presets": [
    { "name": "Vibe Coding", "url": "http://...", "icon": "", "color": "#2932E1" }
  ],
  "general": {
    "language": "zh-CN", "theme": "auto", "ball_size": 48,
    "ball_position": { "x": -1, "y": -1, "screen_index": 0 }
  }
}
```

`schedule.entries` 里的 `day` 用 ISO 整数：1=周一 … 7=周日（与 `datetime.isoweekday()` 一致）。
建议通过设置里的导入功能维护，不要手工编辑。

## 功能说明

### 悬浮球
- 始终置顶，可拖拽到屏幕任意位置，位置变化 > 6px 且距上次保存 > 1s 才写入磁盘
- 单击展开/收起卫星菜单
- 双击直接打开 AI 对话窗口
- 右键菜单：打开设置、退出程序
- 右下角状态指示灯：绿色=正常，黄色=连接中，红色=错误

### 卫星菜单
- 环形展开动画，包含"启动 AI"和预设网址快捷入口
- 点击预设网址以系统默认浏览器打开
- 未安装 PyQtWebEngine 时回退为 webbrowser 模块
- 拖拽悬浮球时菜单实时跟随

### AI 对话
- 支持 OpenAI Chat Completions 兼容接口
- 流式输出，Markdown 渲染，实时滚动
- 多轮对话，保留最近 20 轮上下文
- 文件上传：文本直接读取，二进制 base64 编码
- 模型检测：填写 Base URL 后一键获取可用模型列表

### 多学科系统提示词（13 门学科 + 通用）
- 学科：语文、外语、数学、物理、化学、生物、地理、历史、政治、
  信息科技、人工智能通识、音乐、美术
- 另有不绑定学科的**「通用」**状态：供体育、自习等课程使用，见下节
- 每门学科有独立可编辑的系统提示词，根据学科特点定制
- 提示词统一遵循**苏格拉底式引导**：先判断学生的认知起点，用递进式反问
  引导学生自己推出结论，学生答错时追问其判断依据；
  **每次回答控制在 350 字以内**，内容较多时只给最关键的一步
- **外语学科**适配多语种：英语、日语、俄语、西班牙语
- 同一时间只激活一门学科，AI 对话使用该学科的提示词
- 各学科提示词可修改，保存即生效；支持恢复到默认提示词

### 课程表驱动学科自动切换（V3.1 新增）
- **导入/导出/模板**：设置 → 课程表页签
  - 下载模板：生成 .xlsx，其中「星期」「学科」两列是 Excel 下拉菜单，
    从源头统一输入格式；「开始时间」「结束时间」手工填写，统一 `HH:MM`
  - 导入：容错读取教师手上的各种表格（`8:00` / `8点半` / `8时30分` / Excel 时间格 /
    合并单元格的星期列都会正确解析），未知学科给出相近建议，
    任何一行出错则整体不导入并逐行报告
  - 导出：把当前课程表存成同样带下拉菜单的 xlsx，可改完再导入
- **学科别名兼容**：课程表里写「英语/日语/俄语/西班牙语」会自动归到「外语」，
  「信息技术」→「信息科技」，「AI/人工智能」→「人工智能通识」
- **无固定学科的课程**：体育、自习、班会、升旗、课间操、午休、考试等会归到
  **「通用」**状态——这节课助手不再沿用上一节课的学科，而是切到一段跨学科的
  通用提示词与一组通用开场白，面板标题注明实际课程名（如「AI 助手 · 体育」），
  并清空上一节课的对话历史。自习课上学生问哪一科都能得到回答。
  条目里用 `label` 字段保留原始课程名，导出课程表时写回原名
- **自动切换**：后台每 30 秒比对一次系统时间，在每节课**开始时间前 3 分钟**
  自动把激活学科切到该学科（提前量可在设置中调整），同时
  - 清空当前对话历史（避免 AI 带着上一节课的上下文回答新学科问题）
  - 把对话面板的开场白浮层换成该学科的引导问题
  - 在面板中提示"已按课程表切换到「XX」学科，周X HH:MM–HH:MM"
- 课间与放学后课表不表态，保持当前学科不变（不会反复清空历史）
- 同一时间有重叠记录时以更晚开始的一节为准
- 可在设置中整体关闭该功能

### 开场白引导（按学科）
- **每门学科有各自的引导问题列表**，切换学科时浮层内容随之更换
- 打开对话面板时默认展开（半透明浮层），点击问题自动发起对话
- 在设置 → 开场白引导中按学科编辑（添加/编辑/删除/排序/恢复默认）
- **「开场白引导」与「系统提示词」两个页签共用同一个当前激活学科**：
  在任意一页选择学科，另一页的提示词编辑区与开场白列表都会同步切换，
  保证编辑的内容与实际生效的提示词、开场白指向同一门学科
  （未导入课程表、纯手动选择学科时同样如此）
- 💬 按钮切换展开/折叠

### 连续语音输入（百度 ASR）
- 点击 🎙️ 进入连续语音模式，按钮变红 🔴
- VAD 静音检测自动判断说话结束
- AI 回复完成后自动开始新一轮监听
- 短促噪声自动过滤（< 0.3s），不会中断监听
- 可配置静音阈值 (RMS) 和等待秒数
- 再次点击 🎙️ 退出连续模式

### 语音播报（双引擎可选）
- 全局 🔇/🔉 按钮控制自动播报开关
- 每条 AI 消息旁有 🔊 按钮，点击可单独播报
- **播放状态图标**：🔊 空闲 → ⏳ 合成中（禁用） → ⏹️ 播放中（点击停止）
- 同一时间只有一条音频播放，重复点击停止当前播放
- **两引擎**（设置中切换）：
  - **微软 edge-tts（默认）**：神经网络音质最佳，免费无密钥，需联网
  - **百度云端**：需配置 API Key，支持 9 种发音人
- 播报前自动剥离 Markdown 标记，长文本按句切分后逐段合成播放

### 设置页签（共 7 个）
1. **AI 接口** — LLM 连接配置 + 模型检测
2. **预设网址** — 快捷入口管理（3~6 个）
3. **百度语音识别** — ASR 参数配置
4. **语音合成** — edge-tts / 百度云端切换 + 引擎可用性检测
5. **开场白引导** — 按学科管理引导问题列表
6. **系统提示词** — 按学科管理提示词（与开场白页共用当前激活学科，支持恢复默认）
7. **课程表** — 导入/导出/模板下载 + 自动切换开关与提前分钟数

## 从旧版本升级

`core/config.py` 的 `_migrate()` 在每次加载配置时自动完成：

| 旧配置 | 迁移结果 |
|---|---|
| `baidu_tts` 扁平结构 | → `tts.baidu` |
| `tts.engine` = `local` / `kokoro` / `piper` | → `edge`（本地引擎已移除） |
| 学科「信息技术」 | → 「信息科技」（保留用户已编辑的文本） |
| 只有 12 门学科 | 补齐 13 门（含「人工智能通识」）与「通用」状态 |
| 旧课程表里的体育/自习行 | → 归入「通用」条目并保留原始课程名（此前这类行会被丢弃） |
| 单一 `system_prompt` | → 多学科 `subject_prompts` |
| 全局 `greetings` 列表 | → 挂到当前激活学科名下；用户自定义过的内容不会丢 |
| V3.0 出厂提示词 | → 换成 V3.1 苏格拉底式版本；原文另存于 `panel.subject_prompts_v3_backup` 备查 |

## 打包部署

### 方式一：NSIS 安装程序（推荐）
```bash
# 1. 构建 exe —— 必须用装在纯英文路径下的 Python
pyinstaller build_spec.spec

# 2. 编译安装程序
cd pack
makensis setup.nsi

# 输出: pack/AIAssite-V3.1-Setup.exe
```

> **构建环境的两个硬性要求**（否则会直接报错中止）：
> 1. **Python 必须装在纯英文路径下**。PyQt5 的 `QLibraryInfo` 处理不了中文路径，
>    项目放在含中文的目录（如「开发成品V3.1」）下时，它返回的插件路径会把中文
>    变成 `????`，PyInstaller 的 Qt 钩子随即报 "Qt plugin directory ... does not exist"。
>    若手边只有中文路径下的解释器，把项目复制到 `C:\aiassite_build\` 之类的位置再构建。
> 2. `setup.nsi` 的 `VIProductVersion`/`VIFileVersion` 必须是四段 `X.X.X.X`，
>    脚本里用 `PRODUCT_VERSION_4` 单独定义，不能直接用三段的展示版本号。
>
> 另外，`build_spec.spec` 的 `datas` 只打包 `config/default_settings.json`。
> **不要写成整个 `config` 目录**——那会把含用户 API Key 的 `settings.json`
> 一起打进 exe，分发即泄露密钥。

### 方式二：快速安装（批处理）
```bash
# 1. 构建 exe
pyinstaller build_spec.spec

# 2. 以管理员身份运行
cd pack
install.bat
```

详见 [pack/打包指南.txt](pack/打包指南.txt)

## 技术栈

- **Python** 3.9+，**PyQt5** 5.15+（含 QtMultimedia）
- **httpx** — 流式 HTTP 客户端、百度 API 调用
- **markdown** — Markdown 渲染
- **pyaudio** — 麦克风录音 + VAD 静音检测
- **openpyxl** — 课程表 xlsx 读写与模板下拉菜单
- **edge-tts** — 微软神经网络语音（默认联网引擎）
- **urllib** — 百度 OAuth token 获取（baidu_auth 模块统一提供）
- **PyQtWebEngine**（可选）— 内置浏览器
- **PyInstaller** — 打包为单文件 exe
- **NSIS** — 生成 Windows 安装程序

## V3.1 更新日志

| 变更 | 说明 |
|------|------|
| 课程表自动切换学科 | 新增 `core/schedule.py`：导入课程表后每 30 秒比对系统时间，在每节课开始前 3 分钟自动切换激活学科，并清空对话历史、更换开场白浮层 |
| 课程表导入/导出/模板 | 设置新增「课程表」页签；模板的「星期」「学科」用 Excel 下拉菜单约束输入，时间统一 `HH:MM`；导入容错覆盖 `8:00`/`8点半`/`8时30分`/Excel 时间格/合并单元格等写法 |
| 学科体系调整 | 12 门 → 13 门：新增「人工智能通识」，「信息技术」按课程标准更名为「信息科技」；导入课程表时自动兼容 英语/日语/俄语/西班牙语→外语、信息技术→信息科技、AI→人工智能通识 等写法 |
| 提示词全面重写 | 13 门学科统一改为**苏格拉底式引导**：先判断认知起点，用递进式反问让学生自己推出结论，答错时追问推理依据；**每次回答限定 350 字以内** |
| 开场白按学科下沉 | 全局一份 → 每门学科各自一组专属开场白，切换学科时浮层内容随之更换；用户自定义过的旧开场白自动挂到其当前学科名下 |
| 学科选择联动 | 「系统提示词」页的激活学科与「开场白引导」页的学科合并为同一项，在任意一页切换都会同步另一页，避免编辑的内容与实际生效学科对不上；未导入课程表时纯手动选择也同样联动 |
| 无学科课程处理 | 体育、自习等原先被整行丢弃，导致这些时段助手沿用上一节课的学科（体育课上显示"语文"）。改为归入 **「通用」** 状态：用跨学科提示词与通用开场白，面板注明实际课程名；自习课因此可正常使用；导出课程表时保留原始课程名 |
| 修复悬浮球不可见 | 换显示器或改分辨率后，配置里的旧坐标可能落在所有屏幕之外，程序正常启动但悬浮球看不见；启动时改为校验坐标并回落到主屏默认位置 |
| 提示词与开场白可编辑 | 设置 →「开场白引导」按学科编辑（增删改排序、恢复默认）；「系统提示词」原有编辑能力保留 |
| 移除本地语音合成 | 删除 Kokoro + Piper 双引擎 sidecar 及全部模型（约 598MB）、配套基准与部署脚本；`build_spec.spec`、`setup.nsi`、`requirements.txt` 同步精简 |
| 语音合成精简为两引擎 | 保留 edge-tts（默认，联网）与百度云端；设置页签与「检测当前引擎」同步调整 |
| 配置迁移 | `_migrate()` 新增学科改名/新增、提示词换代（原文备份）、开场白下沉、课程表补全、本地引擎回落 edge |
| 修复并发覆盖 | 设置对话框打开期间若发生课程表自动切换，保存时不再用旧快照覆盖新学科 |
| 打包安全修复 | `build_spec.spec` 原先把整个 `config` 目录打进 exe，会把含 API Key 的 `settings.json` 一起分发出去；改为只打包模板 `default_settings.json` |
| 打包清理 | 移除 spec 中未被源码引用的 `win32api/win32con/win32gui/pywintypes` hiddenimports；`setup.nsi` 的 `VIProductVersion` 改用四段版本号 `PRODUCT_VERSION_4` |
| 版本号 | V3.0.3 → V3.1 |

## V3.0.3 更新日志

| 变更 | 说明 |
|------|------|
| 修复「检测当前引擎」报错 | 该按钮报 "native Qt signal is not callable"：信号发射用了调用式 `sig(x)`，而本环境 PyQt5 只支持 `.emit(x)`。改为 `.emit()`，并保持声明的数据类型与实际一致（dict） |

## V3.0.2 更新日志

| 变更 | 说明 |
|------|------|
| 修复 Piper 合成失败 | 冻结环境缺 4 类资源，逐层修复：g2pW 查找表、中文 tokenizer、transformers 惰性导入、unicode_rbnf 数字规则（V3.1 已移除本地引擎） |
| 修复"必须手动启动服务" | sidecar 改为先监听端口再后台加载引擎，健康检查即时响应（V3.1 已移除本地引擎） |
| 检测按钮可操作性 | 「检测当前引擎」未运行时主动拉起服务并报告结果 |
| 修复关闭面板不停播 | 点击对话面板关闭按钮时停止语音播报 |
| 修复引擎切换变调 | 播放器改用音频流 start 事件的真实采样率创建（V3.1 已移除本地引擎） |
| 服务进程回收 | 设置对话框与对话面板共享服务实例（V3.1 已移除本地引擎） |
| 故障可诊断 | sidecar 请求异常落日志 |
| 构建质量 | 离线包构建内置双引擎冒烟测试（V3.1 已移除本地引擎） |

## V3.0.1 更新日志

| 变更 | 说明 |
|------|------|
| 修复安装后缺本地语音组件 | NSIS 的组件包含判断误用运行期 IfFileExists → 改用编译期 `!if /FileExists`（V3.1 起安装包不再包含本地语音组件） |
| 卸载清理 | 卸载时清理 tts_server 目录并结束 sidecar 进程 |

## V3.0 更新日志

| 变更 | 说明 |
|------|------|
| 四引擎语音合成 | 微软 edge-tts / 本地 Kokoro / 本地 Piper / 百度云端（V3.1 精简为前两者中的 edge 与百度） |
| 本地双引擎 sidecar | `tts_server/server.py` 独立服务进程（V3.1 已移除） |
| 多语种播报 | 中/英/日/俄/西等混读（V3.1 已移除） |
| 文本归一化 | 英文单词/缩写自动转中文读法（V3.1 已移除） |
| 配置结构升级 | `baidu_tts` 扁平结构 → `tts` 多引擎结构，旧配置自动迁移 |
| 设置界面 | 语音合成页签四引擎切换 + 引擎专属参数面板 + 本地服务状态检查 |
| 修复 | 清空对话时停止语音播报；修改服务端口即时生效；设置保存崩溃（布局双父） |
| 版权标注 | 关于对话框 / 托盘提示 / 安装程序统一标注 Copyright (c) 2026 YINJUN |
| 版本号 | V2.0 → V3.0 |

## V2.0 更新日志

| 变更 | 说明 |
|------|------|
| 多学科提示词 | 单一系统提示词 → 12 学科独立提示词，可切换激活 |
| 外语多语种 | 外语提示词覆盖日语/俄语/英语/西班牙语 |
| TTS 状态图标 | 🔊 空闲 → ⏳ 合成中 → ⏹️ 播放中（可停止） |
| TTS 防重叠 | 同一时间只允许一条音频播放 |
| 百度认证共享 | 提取 baidu_auth.py，ASR/TTS 共用 fetch_token |
| 原子配置写入 | 先写 .tmp 再 rename，防止配置损坏 |
| 悬浮球位置保存 | 6px + 1s 防抖，减少磁盘写入 |
| 版本号 | V1.0 → V2.0 |
| 授权协议 | MIT → 个人使用授权，版权归属 YINJUN |
| 打包方式 | 源代码分发 → PyInstaller 单文件 exe + NSIS 安装程序 |

## 故障排除

| 问题 | 解决方案 |
|------|---------|
| 悬浮球不显示 | 系统托盘右键 → "显示悬浮球" |
| AI 调用失败 | 检查设置中的 API Key 和 Base URL |
| 语音识别失败 3314 | 正常，短促噪声已自动过滤并恢复监听 |
| 语音识别失败 3307 | 检查音频格式和采样率配置 |
| 语音播报无声 | edge-tts 需联网；改用百度云端需检查 API Key/Secret Key |
| 语音播报重叠 | 重复点击会停止当前播放 |
| 课程表导入报"文件被占用" | 先在 Excel 中关闭该文件再导入 |
| 课程表导入报时间格式错误 | 按 `HH:MM` 填写（如 `08:00`）；模板的「说明」表里有写法对照 |
| 课程表学科无法识别 | 用模板下拉菜单里的标准学科名；系统已兼容常见别名 |
| 学科没有按时间切换 | 检查设置 → 课程表中「启用课程表自动切换学科」是否勾选、当天是否排了课 |
| 开机自启动无效 | 检查任务管理器启动选项卡，或手动 Win+R → shell:startup |
| 依赖安装失败 | `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple` |
| NSIS 编译中文乱码 | setup.nsi 需 UTF-8 BOM，License 文件也需 UTF-8 BOM |
